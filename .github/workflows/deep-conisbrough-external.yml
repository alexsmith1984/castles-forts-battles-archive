name: Conisbrough external archive search

on:
  push:
    paths:
      - '.github/workflows/deep-conisbrough-external.yml'

permissions:
  contents: write

jobs:
  recover:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests beautifulsoup4 pillow
      - run: python scripts/deep_conisbrough_external_archives.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add recovered/conisbrough-castle
          if git diff --cached --quiet; then
            echo 'No Conisbrough archive changes'
          else
            git commit -m 'Complete fresh Conisbrough external archive search'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
