name: Barnard Castle external archive search
on:
  push:
    paths:
      - '.github/workflows/deep-barnard-external.yml'
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: barnard-castle-external
  cancel-in-progress: true
jobs:
  recover:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests pillow
      - run: python scripts/deep_barnard_external_archives.py
      - run: python scripts/verify_barnard_castle.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add recovered/barnard-castle
          if git diff --cached --quiet; then echo 'No page changes'; else
            git commit -m 'Search external archives for Barnard Castle images'
            git pull --rebase origin main
            git push origin HEAD:main
          fi
