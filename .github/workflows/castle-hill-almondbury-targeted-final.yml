name: Castle Hill Almondbury targeted final recovery
on:
  push:
    paths:
      - '.github/workflows/castle-hill-almondbury-targeted-final.yml'
permissions:
  contents: write
jobs:
  recover:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests pillow beautifulsoup4
      - run: python scripts/recover_castle_hill_almondbury_targeted_final.py
      - name: Commit final Almondbury recovery
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add recovered/castle-hill-almondbury page-audit/castle-hill-almondbury.json
          if git diff --cached --quiet; then echo 'No Almondbury changes'; else git commit -m 'Finalize Castle Hill Almondbury recovery'; git pull --rebase origin main; git push origin HEAD:main; fi
